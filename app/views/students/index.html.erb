<h1>Students Index</h1>
<%= button_to "Add new student", new_student,path, method: :get %>

<ul>
    <% @students.each do |student| %>
        <li>
        <%= link_to student.name, student_path(student)%>
        </li>
</ul>